export const metadata = {
    title: "Buy Carbon Credits from Farmers",
    description: "Browse available carbon credit listings from farmers around the world. Filter by country, state, and district to find verified carbon offsets.",
};

export default function BuyLayout({ children }) {
    return <>{children}</>;
}
